{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../.. && npm install && npm run build --workspace=packages/core && npm run build --workspace=apps/web",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "echo skip-install-handled-by-buildCommand",
  "env": {
    "NEXT_PUBLIC_API_URL": "@duskvorn_api_url"
  }
}
